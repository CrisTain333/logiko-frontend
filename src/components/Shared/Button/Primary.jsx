import React from 'react';

const Primary = () => {
    return (
      <button className="bg-gradient-to-r from-[#005DFF] to-[#0091FF] py-2 px-4 rounded-full text-white font-semibold text-xs">
        Confirm
      </button>
    );
};

export default Primary;